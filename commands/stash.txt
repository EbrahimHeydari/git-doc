# دستورات stash

git stash                                           // تغییرات را به صورت موقت در جای دیگری ذخیره می کند تا بعدا بازیابی بشن

git stash save 'message'                            // ذخیره با نام دلخواه برای شناسایی راحت تر

